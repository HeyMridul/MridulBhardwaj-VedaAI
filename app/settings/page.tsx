import PlaceholderPage from "@/components/layout/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      body="School profile and AI provider settings can be added later. Demo mode works without an API key."
    />
  );
}
